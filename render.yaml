services:
  - type: web
    name: rascarobingo
    buildCommand: npm run build
    publishDir: dist
    staticPublishing: true
    envVars:
      - key: NODE_VERSION
        value: 18.17.0
    routes:
      - type: rewrite
        source: /*
        destination: /index.html